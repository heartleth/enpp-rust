library standard

When it starts
    Let s as input_line:()

    s filter! (|e| e is "("[0] or e is ")"[0])

    ret map! (|e| if e is "("[0] then 1 else -1)

    ret integrate! (|a, b|(a + b))

    ret fold! (|a, b| if a > b then a else b)